# Routes

API route handlers organized by feature:

- `auth.ts` - Authentication routes (GitHub OAuth, JWT)
- `resources.ts` - Resource management routes (browse, download, upload)
- `users.ts` - User-related routes (mileage, profile)
- `admin.ts` - Admin routes (approve/reject resources)
- `health.ts` - Health check endpoint
